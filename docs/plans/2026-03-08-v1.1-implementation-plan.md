# Prayer Room v1.1 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add i18n (FR/EN), custom interval picker, alarm display templates, full offline Bible with thematic verse index, and native alarm sounds to Prayer Room.

**Architecture:** i18n is the foundation (all UI strings become t() calls). Then custom interval (small self-contained change). Then alarm templates (new template components + Alarm type update). Then Bible data + browser (largest feature). Audio files sourced last.

**Tech Stack:** React Native + Expo SDK 55, Expo Router, TypeScript, AsyncStorage, expo-localization, expo-linear-gradient, Animated API, getBible v2 JSON data.

---

## Task 1: i18n Foundation — Language Context + Translation Files

**Files:**
- Create: `src/i18n/fr.json`
- Create: `src/i18n/en.json`
- Create: `src/i18n/index.ts`
- Create: `src/contexts/LanguageContext.tsx`
- Modify: `src/types/index.ts:32-36` (add language to Settings)
- Modify: `app/_layout.tsx:51-57` (wrap with LanguageProvider)

**Step 1: Create French translation file**

Create `src/i18n/fr.json`:
```json
{
  "app.name": "Prayer Room",
  "tabs.home": "Accueil",
  "tabs.packs": "Versets",
  "tabs.settings": "Reglages",
  "home.title": "Prayer Room",
  "home.alarms": "Alarmes",
  "home.verse_placeholder": "Votre verset du jour apparaitra ici.",
  "home.add_first_alarm": "Ajouter votre premiere alarme",
  "alarm.new_title": "Nouvelle alarme",
  "alarm.edit_title": "Modifier l'alarme",
  "alarm.name_section": "NOM",
  "alarm.name_default": "Ma priere",
  "alarm.interval_section": "INTERVALLE",
  "alarm.interval_other": "Autre",
  "alarm.interval_custom_title": "Intervalle personnalise",
  "alarm.interval_validate": "Valider",
  "alarm.time_section": "HORAIRES",
  "alarm.time_start": "Debut",
  "alarm.time_end": "Fin",
  "alarm.audio_section": "SON",
  "alarm.audio_import": "Importer un audio",
  "alarm.packs_section": "VERSETS",
  "alarm.style_section": "STYLE",
  "alarm.save_new": "Creer l'alarme",
  "alarm.save_edit": "Enregistrer",
  "alarm.delete": "Supprimer l'alarme",
  "alarm.delete_confirm": "Supprimer cette alarme ?",
  "alarm.template.standard": "Standard",
  "alarm.template.incoming_call": "Appel",
  "alarm.template.wake_up": "Reveil",
  "alarm.template.immersive": "Immersif",
  "triggered.silence": "Silence",
  "triggered.close": "Fermer",
  "triggered.pray": "Prier",
  "triggered.later": "Plus tard",
  "triggered.snooze": "Rappel dans 5 min",
  "triggered.stop": "Arreter",
  "triggered.slide_to_pray": "Glisser pour prier",
  "triggered.finish": "Terminer",
  "packs.title": "Versets",
  "packs.empty": "Aucun pack disponible.",
  "packs.create": "Creer un pack",
  "packs.verses_count": "{count} versets",
  "packs.browse_bible": "Parcourir la Bible",
  "packs.edit": "Modifier le pack",
  "packs.new": "Nouveau pack",
  "packs.name_label": "Nom",
  "packs.name_placeholder": "Ex: Mes versets preferes",
  "packs.desc_label": "Description",
  "packs.desc_placeholder": "Courte description",
  "packs.add_verse_section": "AJOUTER UN VERSET",
  "packs.ref_placeholder": "Reference (ex: Jean 3:16)",
  "packs.text_placeholder": "Texte du verset",
  "packs.add": "Ajouter",
  "packs.verses_section": "VERSETS",
  "packs.save_new": "Creer le pack",
  "packs.save_edit": "Enregistrer",
  "packs.delete": "Supprimer le pack",
  "packs.delete_confirm": "Supprimer \"{name}\" et tous ses versets ?",
  "packs.delete_verse_confirm": "Supprimer \"{ref}\" ?",
  "packs.add_verse_btn": "Ajouter un verset",
  "bible.title": "Bible",
  "bible.search_placeholder": "Chercher un verset...",
  "bible.old_testament": "Ancien Testament",
  "bible.new_testament": "Nouveau Testament",
  "bible.add_to_pack": "Ajouter au pack",
  "bible.added": "Ajoute !",
  "bible.translation": "Traduction",
  "settings.title": "Reglages",
  "settings.appearance": "APPARENCE",
  "settings.theme": "Theme",
  "settings.theme_dark": "Sombre",
  "settings.theme_light": "Clair",
  "settings.language": "LANGUE",
  "settings.language_label": "Langue",
  "settings.language_fr": "Francais",
  "settings.language_en": "English",
  "settings.focus": "MODE FOCUS",
  "settings.focus_configure": "Configurer le Focus",
  "settings.focus_desc": "Bloquer les notifications pendant la priere",
  "settings.support": "SOUTIEN",
  "settings.about": "A PROPOS",
  "settings.version": "Version",
  "settings.made_with_love": "Fait avec amour pour la communaute",
  "settings.iap_unavailable": "Les achats integres ne sont pas encore disponibles. Merci pour votre generosite !",
  "settings.thank_you": "Votre soutien est une vraie benediction. Que Dieu vous benisse !",
  "common.cancel": "Annuler",
  "common.delete": "Supprimer",
  "common.loading": "Chargement...",
  "common.required_fields": "Champs requis",
  "common.name_required": "Veuillez donner un nom au pack.",
  "common.coming_soon": "Bientot disponible",
  "category.healing": "Guerison",
  "category.encouragement": "Encouragement",
  "category.prosperity": "Prosperite",
  "category.faith": "Foi",
  "category.protection": "Protection",
  "category.love": "Amour",
  "category.wisdom": "Sagesse",
  "category.praise": "Louange",
  "category.peace": "Paix",
  "category.family": "Famille",
  "category.work": "Travail",
  "category.forgiveness": "Pardon",
  "category.strength": "Force",
  "category.joy": "Joie",
  "category.custom": "Personnalise",
  "audio.default": "Son par defaut",
  "audio.tri_tone": "Tri-tone",
  "audio.bell": "Cloche",
  "audio.chime": "Carillon",
  "audio.harp": "Harpe",
  "audio.shofar": "Shofar",
  "audio.gentle_ping": "Ping doux",
  "audio.worship_piano_1": "Piano Worship 1",
  "audio.worship_piano_2": "Piano Worship 2",
  "audio.gentle_worship": "Louange Douce",
  "audio.prayer_ambient": "Ambiance Priere",
  "audio.meditation_calm": "Meditation Calme",
  "audio.custom": "Audio personnalise",
  "audio.import": "Importer un audio"
}
```

**Step 2: Create English translation file**

Create `src/i18n/en.json`:
```json
{
  "app.name": "Prayer Room",
  "tabs.home": "Home",
  "tabs.packs": "Verses",
  "tabs.settings": "Settings",
  "home.title": "Prayer Room",
  "home.alarms": "Alarms",
  "home.verse_placeholder": "Your daily verse will appear here.",
  "home.add_first_alarm": "Add your first alarm",
  "alarm.new_title": "New Alarm",
  "alarm.edit_title": "Edit Alarm",
  "alarm.name_section": "NAME",
  "alarm.name_default": "My prayer",
  "alarm.interval_section": "INTERVAL",
  "alarm.interval_other": "Other",
  "alarm.interval_custom_title": "Custom interval",
  "alarm.interval_validate": "Confirm",
  "alarm.time_section": "SCHEDULE",
  "alarm.time_start": "Start",
  "alarm.time_end": "End",
  "alarm.audio_section": "SOUND",
  "alarm.audio_import": "Import audio",
  "alarm.packs_section": "VERSES",
  "alarm.style_section": "STYLE",
  "alarm.save_new": "Create alarm",
  "alarm.save_edit": "Save",
  "alarm.delete": "Delete alarm",
  "alarm.delete_confirm": "Delete this alarm?",
  "alarm.template.standard": "Standard",
  "alarm.template.incoming_call": "Call",
  "alarm.template.wake_up": "Wake Up",
  "alarm.template.immersive": "Immersive",
  "triggered.silence": "Silence",
  "triggered.close": "Close",
  "triggered.pray": "Pray",
  "triggered.later": "Later",
  "triggered.snooze": "Remind in 5 min",
  "triggered.stop": "Stop",
  "triggered.slide_to_pray": "Slide to pray",
  "triggered.finish": "Finish",
  "packs.title": "Verses",
  "packs.empty": "No packs available.",
  "packs.create": "Create a pack",
  "packs.verses_count": "{count} verses",
  "packs.browse_bible": "Browse the Bible",
  "packs.edit": "Edit pack",
  "packs.new": "New pack",
  "packs.name_label": "Name",
  "packs.name_placeholder": "E.g. My favorite verses",
  "packs.desc_label": "Description",
  "packs.desc_placeholder": "Short description",
  "packs.add_verse_section": "ADD A VERSE",
  "packs.ref_placeholder": "Reference (e.g. John 3:16)",
  "packs.text_placeholder": "Verse text",
  "packs.add": "Add",
  "packs.verses_section": "VERSES",
  "packs.save_new": "Create pack",
  "packs.save_edit": "Save",
  "packs.delete": "Delete pack",
  "packs.delete_confirm": "Delete \"{name}\" and all its verses?",
  "packs.delete_verse_confirm": "Delete \"{ref}\"?",
  "packs.add_verse_btn": "Add a verse",
  "bible.title": "Bible",
  "bible.search_placeholder": "Search for a verse...",
  "bible.old_testament": "Old Testament",
  "bible.new_testament": "New Testament",
  "bible.add_to_pack": "Add to pack",
  "bible.added": "Added!",
  "bible.translation": "Translation",
  "settings.title": "Settings",
  "settings.appearance": "APPEARANCE",
  "settings.theme": "Theme",
  "settings.theme_dark": "Dark",
  "settings.theme_light": "Light",
  "settings.language": "LANGUAGE",
  "settings.language_label": "Language",
  "settings.language_fr": "Francais",
  "settings.language_en": "English",
  "settings.focus": "FOCUS MODE",
  "settings.focus_configure": "Configure Focus",
  "settings.focus_desc": "Block notifications during prayer",
  "settings.support": "SUPPORT",
  "settings.about": "ABOUT",
  "settings.version": "Version",
  "settings.made_with_love": "Made with love for the community",
  "settings.iap_unavailable": "In-app purchases are not available yet. Thank you for your generosity!",
  "settings.thank_you": "Your support is a true blessing. God bless you!",
  "common.cancel": "Cancel",
  "common.delete": "Delete",
  "common.loading": "Loading...",
  "common.required_fields": "Required fields",
  "common.name_required": "Please name your pack.",
  "common.coming_soon": "Coming soon",
  "category.healing": "Healing",
  "category.encouragement": "Encouragement",
  "category.prosperity": "Prosperity",
  "category.faith": "Faith",
  "category.protection": "Protection",
  "category.love": "Love",
  "category.wisdom": "Wisdom",
  "category.praise": "Praise",
  "category.peace": "Peace",
  "category.family": "Family",
  "category.work": "Work",
  "category.forgiveness": "Forgiveness",
  "category.strength": "Strength",
  "category.joy": "Joy",
  "category.custom": "Custom",
  "audio.default": "Default sound",
  "audio.tri_tone": "Tri-tone",
  "audio.bell": "Bell",
  "audio.chime": "Chime",
  "audio.harp": "Harp",
  "audio.shofar": "Shofar",
  "audio.gentle_ping": "Gentle ping",
  "audio.worship_piano_1": "Worship Piano 1",
  "audio.worship_piano_2": "Worship Piano 2",
  "audio.gentle_worship": "Gentle Worship",
  "audio.prayer_ambient": "Prayer Ambient",
  "audio.meditation_calm": "Calm Meditation",
  "audio.custom": "Custom audio",
  "audio.import": "Import audio"
}
```

**Step 3: Create i18n index helper**

Create `src/i18n/index.ts`:
```typescript
import fr from './fr.json';
import en from './en.json';

export type Language = 'fr' | 'en';

const translations: Record<Language, Record<string, string>> = { fr, en };

export function translate(language: Language, key: string, params?: Record<string, string | number>): string {
  let text = translations[language][key] ?? translations['en'][key] ?? key;
  if (params) {
    Object.entries(params).forEach(([k, v]) => {
      text = text.replace(`{${k}}`, String(v));
    });
  }
  return text;
}
```

**Step 4: Create LanguageContext**

Create `src/contexts/LanguageContext.tsx`:
```typescript
import React, { createContext, useContext, useState, useEffect, useCallback } from 'react';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { Language, translate } from '../i18n';

let Localization: any = null;
try {
  Localization = require('expo-localization');
} catch {}

interface LanguageContextType {
  language: Language;
  setLanguage: (lang: Language) => void;
  t: (key: string, params?: Record<string, string | number>) => string;
}

const LanguageContext = createContext<LanguageContextType | undefined>(undefined);

function detectLanguage(): Language {
  try {
    const locale = Localization?.getLocales?.()?.[0]?.languageCode;
    return locale === 'fr' ? 'fr' : 'en';
  } catch {
    return 'fr';
  }
}

export function LanguageProvider({ children }: { children: React.ReactNode }) {
  const [language, setLanguageState] = useState<Language>(detectLanguage());

  useEffect(() => {
    AsyncStorage.getItem('language').then((saved) => {
      if (saved === 'fr' || saved === 'en') setLanguageState(saved);
    });
  }, []);

  const setLanguage = useCallback((lang: Language) => {
    setLanguageState(lang);
    AsyncStorage.setItem('language', lang);
  }, []);

  const t = useCallback(
    (key: string, params?: Record<string, string | number>) => translate(language, key, params),
    [language]
  );

  return (
    <LanguageContext.Provider value={{ language, t, setLanguage }}>
      {children}
    </LanguageContext.Provider>
  );
}

export function useLanguage() {
  const ctx = useContext(LanguageContext);
  if (!ctx) throw new Error('useLanguage must be used within LanguageProvider');
  return ctx;
}
```

**Step 5: Update Settings type**

Modify `src/types/index.ts:32-36` — add `language` field:
```typescript
export interface Settings {
  theme: 'dark' | 'light';
  language: 'fr' | 'en';
  lastVerseIndex: Record<string, number>;
}
```

**Step 6: Wrap app with LanguageProvider**

Modify `app/_layout.tsx:51-57`:
```typescript
import { LanguageProvider } from '../src/contexts/LanguageContext';

export default function RootLayout() {
  return (
    <LanguageProvider>
      <ThemeProvider>
        <RootLayoutInner />
      </ThemeProvider>
    </LanguageProvider>
  );
}
```

**Step 7: Install expo-localization**

Run: `npx expo install expo-localization`

**Step 8: Verify TypeScript compiles**

Run: `npx tsc --noEmit`
Expected: no errors

**Step 9: Commit**

```bash
git add src/i18n/ src/contexts/LanguageContext.tsx src/types/index.ts app/_layout.tsx package.json
git commit -m "feat: add i18n foundation with FR/EN translations and LanguageContext"
```

---

## Task 2: Migrate All Screens to i18n

**Files:**
- Modify: `app/(tabs)/index.tsx` (all hardcoded French strings)
- Modify: `app/(tabs)/settings.tsx` (all hardcoded French strings + add language selector)
- Modify: `app/(tabs)/packs/index.tsx`
- Modify: `app/(tabs)/packs/[id].tsx`
- Modify: `app/(tabs)/packs/create.tsx`
- Modify: `app/alarm/[id].tsx`
- Modify: `app/alarm-triggered.tsx`
- Modify: `src/components/AudioPicker.tsx`
- Modify: `src/data/builtin-audio.ts`

**Step 1: Add `useLanguage()` to every screen**

In every screen/component file, add:
```typescript
import { useLanguage } from '../../src/contexts/LanguageContext'; // adjust path
// inside component:
const { t } = useLanguage();
```

Then replace every hardcoded string. Examples:
- `"Prayer Room"` → `{t('home.title')}`
- `"Alarmes"` → `{t('home.alarms')}`
- `"Reglages"` → `{t('settings.title')}`
- `"Nouvelle alarme"` → `{t('alarm.new_title')}`
- etc.

**Step 2: Add language selector to Settings**

In `app/(tabs)/settings.tsx`, add a new section after APPARENCE:

```tsx
{/* Language */}
<Text style={[styles.sectionLabel, { color: colors.textMuted }]}>{t('settings.language')}</Text>
<View style={[styles.groupCard, { backgroundColor: colors.card, borderColor: colors.borderLight }]}>
  <Pressable onPress={() => setLanguage('fr')} style={styles.row}>
    <Text style={[styles.rowLabel, { color: colors.text }]}>{t('settings.language_fr')}</Text>
    {language === 'fr' && <Text style={[styles.checkmark, { color: colors.primary }]}>{'\u2713'}</Text>}
  </Pressable>
  <View style={[styles.rowSeparator, { backgroundColor: colors.borderLight }]} />
  <Pressable onPress={() => setLanguage('en')} style={styles.row}>
    <Text style={[styles.rowLabel, { color: colors.text }]}>{t('settings.language_en')}</Text>
    {language === 'en' && <Text style={[styles.checkmark, { color: colors.primary }]}>{'\u2713'}</Text>}
  </Pressable>
</View>
```

**Step 3: Update AudioPicker to use translation keys**

Modify `src/data/builtin-audio.ts` to use i18n keys instead of hardcoded names:
```typescript
export const NATIVE_SOUNDS: AudioSource[] = [
  { type: 'native', soundId: 'default', name: 'audio.default' },
  { type: 'native', soundId: 'tri-tone', name: 'audio.tri_tone' },
  { type: 'native', soundId: 'bell', name: 'audio.bell' },
  { type: 'native', soundId: 'chime', name: 'audio.chime' },
  { type: 'native', soundId: 'harp', name: 'audio.harp' },
  { type: 'native', soundId: 'shofar', name: 'audio.shofar' },
  { type: 'native', soundId: 'gentle-ping', name: 'audio.gentle_ping' },
];
```

Then in AudioPicker, render with `t(item.name)` instead of `item.name`.

**Step 4: Verify TypeScript compiles**

Run: `npx tsc --noEmit`

**Step 5: Commit**

```bash
git add -A
git commit -m "feat: migrate all screens to i18n with language selector in settings"
```

---

## Task 3: Custom Interval Picker

**Files:**
- Modify: `src/components/IntervalPicker.tsx`

**Step 1: Add "Autre" pill + modal with scroll pickers**

Replace `src/components/IntervalPicker.tsx` with:
```typescript
import React, { useState } from 'react';
import { View, Text, ScrollView, Pressable, Modal, StyleSheet } from 'react-native';
import { Picker } from '@react-native-community/picker'; // or custom scroll
import { useTheme } from '../contexts/ThemeContext';
import { useLanguage } from '../contexts/LanguageContext';
import { Spacing, FontSize, BorderRadius } from '../constants/theme';

const PRESETS = [
  { label: '30 min', minutes: 30 },
  { label: '40 min', minutes: 40 },
  { label: '1h', minutes: 60 },
  { label: '1h30', minutes: 90 },
  { label: '2h', minutes: 120 },
  { label: '3h', minutes: 180 },
];

interface IntervalPickerProps {
  value: number;
  onChange: (minutes: number) => void;
}

function formatCustomInterval(minutes: number): string {
  const h = Math.floor(minutes / 60);
  const m = minutes % 60;
  if (h === 0) return `${m} min`;
  if (m === 0) return `${h}h`;
  return `${h}h${m.toString().padStart(2, '0')}`;
}

export function IntervalPicker({ value, onChange }: IntervalPickerProps) {
  const { colors } = useTheme();
  const { t } = useLanguage();
  const [showModal, setShowModal] = useState(false);
  const [tempHours, setTempHours] = useState(Math.floor(value / 60));
  const [tempMinutes, setTempMinutes] = useState(value % 60);

  const isPreset = PRESETS.some((p) => p.minutes === value);
  const customLabel = isPreset ? t('alarm.interval_other') : formatCustomInterval(value);

  function handleConfirm() {
    const total = tempHours * 60 + tempMinutes;
    onChange(Math.max(5, total)); // minimum 5 min
    setShowModal(false);
  }

  function handleOpenCustom() {
    setTempHours(Math.floor(value / 60));
    setTempMinutes(value % 60);
    setShowModal(true);
  }

  return (
    <View style={styles.container}>
      <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.scrollContent}>
        {PRESETS.map((preset) => {
          const selected = value === preset.minutes;
          return (
            <Pressable
              key={preset.minutes}
              onPress={() => onChange(preset.minutes)}
              style={[styles.pill, {
                backgroundColor: selected ? colors.primary : colors.card,
                borderColor: selected ? colors.primary : colors.borderLight,
              }]}
            >
              <Text style={[styles.pillText, { color: selected ? '#000000' : colors.text }]}>
                {preset.label}
              </Text>
            </Pressable>
          );
        })}
        {/* Custom pill */}
        <Pressable
          onPress={handleOpenCustom}
          style={[styles.pill, {
            backgroundColor: !isPreset ? colors.primary : colors.card,
            borderColor: !isPreset ? colors.primary : colors.borderLight,
          }]}
        >
          <Text style={[styles.pillText, { color: !isPreset ? '#000000' : colors.text }]}>
            {customLabel}
          </Text>
        </Pressable>
      </ScrollView>

      {/* Custom interval modal */}
      <Modal visible={showModal} transparent animationType="slide">
        <Pressable style={styles.modalOverlay} onPress={() => setShowModal(false)}>
          <Pressable style={[styles.modalContent, { backgroundColor: colors.card }]} onPress={() => {}}>
            <Text style={[styles.modalTitle, { color: colors.text }]}>{t('alarm.interval_custom_title')}</Text>
            <View style={styles.pickerRow}>
              <View style={styles.pickerCol}>
                <Text style={[styles.pickerLabel, { color: colors.textMuted }]}>h</Text>
                <ScrollView style={styles.pickerScroll} showsVerticalScrollIndicator={false}>
                  {Array.from({ length: 13 }, (_, i) => i).map((h) => (
                    <Pressable key={h} onPress={() => setTempHours(h)} style={[styles.pickerItem, tempHours === h && { backgroundColor: colors.accentSoft }]}>
                      <Text style={[styles.pickerItemText, { color: tempHours === h ? colors.primary : colors.text }]}>{h}</Text>
                    </Pressable>
                  ))}
                </ScrollView>
              </View>
              <View style={styles.pickerCol}>
                <Text style={[styles.pickerLabel, { color: colors.textMuted }]}>min</Text>
                <ScrollView style={styles.pickerScroll} showsVerticalScrollIndicator={false}>
                  {Array.from({ length: 12 }, (_, i) => i * 5).map((m) => (
                    <Pressable key={m} onPress={() => setTempMinutes(m)} style={[styles.pickerItem, tempMinutes === m && { backgroundColor: colors.accentSoft }]}>
                      <Text style={[styles.pickerItemText, { color: tempMinutes === m ? colors.primary : colors.text }]}>{m.toString().padStart(2, '0')}</Text>
                    </Pressable>
                  ))}
                </ScrollView>
              </View>
            </View>
            <Pressable onPress={handleConfirm} style={[styles.confirmButton, { backgroundColor: colors.primary }]}>
              <Text style={styles.confirmText}>{t('alarm.interval_validate')}</Text>
            </Pressable>
          </Pressable>
        </Pressable>
      </Modal>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { marginBottom: Spacing.lg },
  scrollContent: { paddingHorizontal: Spacing.md, gap: Spacing.sm },
  pill: { paddingHorizontal: Spacing.lg, paddingVertical: Spacing.sm + 2, borderRadius: BorderRadius.full, borderWidth: 0.33 },
  pillText: { fontSize: FontSize.sm, fontWeight: '500' },
  modalOverlay: { flex: 1, justifyContent: 'flex-end', backgroundColor: 'rgba(0,0,0,0.5)' },
  modalContent: { borderTopLeftRadius: 20, borderTopRightRadius: 20, padding: Spacing.lg, paddingBottom: Spacing.xxxl },
  modalTitle: { fontSize: FontSize.xl, fontWeight: '600', textAlign: 'center', marginBottom: Spacing.lg },
  pickerRow: { flexDirection: 'row', justifyContent: 'center', gap: Spacing.xxl },
  pickerCol: { alignItems: 'center', width: 80 },
  pickerLabel: { fontSize: FontSize.sm, fontWeight: '500', marginBottom: Spacing.sm },
  pickerScroll: { maxHeight: 200 },
  pickerItem: { paddingVertical: Spacing.sm, paddingHorizontal: Spacing.lg, borderRadius: BorderRadius.sm, marginVertical: 2 },
  pickerItemText: { fontSize: FontSize.xl, fontWeight: '500', textAlign: 'center' },
  confirmButton: { marginTop: Spacing.lg, paddingVertical: Spacing.md, borderRadius: BorderRadius.md, alignItems: 'center' },
  confirmText: { fontSize: FontSize.md, fontWeight: '600', color: '#000000' },
});
```

**Step 2: Verify TypeScript compiles**

Run: `npx tsc --noEmit`

**Step 3: Commit**

```bash
git add src/components/IntervalPicker.tsx
git commit -m "feat: add custom interval picker with 'Autre' pill and bottom sheet modal"
```

---

## Task 4: Alarm Templates — Type Updates + Template Selector

**Files:**
- Modify: `src/types/index.ts:6-16` (add template field to Alarm)
- Create: `src/components/TemplatePicker.tsx`
- Modify: `app/alarm/[id].tsx` (add STYLE section with TemplatePicker)

**Step 1: Add template to Alarm type**

Modify `src/types/index.ts`:
```typescript
export type AlarmTemplate = 'standard' | 'incoming-call' | 'wake-up' | 'immersive';

export interface Alarm {
  id: string;
  name: string;
  intervalMinutes: number;
  startTime: string;
  endTime: string;
  audio: AudioSource;
  packId: string;
  enabled: boolean;
  createdAt: string;
  template: AlarmTemplate;
}
```

**Step 2: Create TemplatePicker component**

Create `src/components/TemplatePicker.tsx`:
```typescript
import React from 'react';
import { View, Text, Pressable, StyleSheet } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useTheme } from '../contexts/ThemeContext';
import { useLanguage } from '../contexts/LanguageContext';
import { AlarmTemplate } from '../types';
import { Spacing, FontSize, BorderRadius } from '../constants/theme';

interface TemplatePickerProps {
  value: AlarmTemplate;
  onChange: (template: AlarmTemplate) => void;
}

const TEMPLATES: { id: AlarmTemplate; icon: string; labelKey: string }[] = [
  { id: 'standard', icon: 'reader-outline', labelKey: 'alarm.template.standard' },
  { id: 'incoming-call', icon: 'call-outline', labelKey: 'alarm.template.incoming_call' },
  { id: 'wake-up', icon: 'alarm-outline', labelKey: 'alarm.template.wake_up' },
  { id: 'immersive', icon: 'sparkles-outline', labelKey: 'alarm.template.immersive' },
];

export function TemplatePicker({ value, onChange }: TemplatePickerProps) {
  const { colors } = useTheme();
  const { t } = useLanguage();

  return (
    <View style={styles.container}>
      {TEMPLATES.map((tmpl) => {
        const selected = value === tmpl.id;
        return (
          <Pressable
            key={tmpl.id}
            onPress={() => onChange(tmpl.id)}
            style={[
              styles.card,
              {
                backgroundColor: selected ? colors.accentSoft : colors.card,
                borderColor: selected ? colors.primary : colors.borderLight,
                borderWidth: selected ? 1.5 : 0.33,
              },
            ]}
          >
            <Ionicons
              name={tmpl.icon as any}
              size={24}
              color={selected ? colors.primary : colors.textSecondary}
            />
            <Text
              style={[
                styles.label,
                { color: selected ? colors.primary : colors.textSecondary },
              ]}
              numberOfLines={1}
            >
              {t(tmpl.labelKey)}
            </Text>
          </Pressable>
        );
      })}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    paddingHorizontal: Spacing.md,
    gap: Spacing.sm,
  },
  card: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: Spacing.md,
    borderRadius: BorderRadius.md,
    gap: Spacing.xs,
  },
  label: {
    fontSize: FontSize.xs,
    fontWeight: '500',
    textAlign: 'center',
  },
});
```

**Step 3: Add STYLE section to alarm form**

In `app/alarm/[id].tsx`, add:
- Import TemplatePicker
- Add state: `const [template, setTemplate] = useState<AlarmTemplate>('standard');`
- Add section between VERSETS and Save button:
```tsx
{/* Style */}
<View style={styles.section}>
  <Text style={[styles.sectionHeader, { color: colors.textMuted }]}>{t('alarm.style_section')}</Text>
  <TemplatePicker value={template} onChange={setTemplate} />
</View>
```
- Include `template` in the alarm object in `handleSave()`
- Load template from existing alarm in `useEffect`

**Step 4: Set default template for existing alarms**

In `src/storage/alarms.ts`, add migration: when loading alarms, if `template` is missing, default to `'standard'`.

**Step 5: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add src/types/index.ts src/components/TemplatePicker.tsx app/alarm/[id].tsx src/storage/alarms.ts
git commit -m "feat: add alarm template picker with 4 display styles"
```

---

## Task 5: Alarm Templates — Implement 4 Template Screens

**Files:**
- Create: `src/components/alarm-templates/StandardTemplate.tsx`
- Create: `src/components/alarm-templates/IncomingCallTemplate.tsx`
- Create: `src/components/alarm-templates/WakeUpTemplate.tsx`
- Create: `src/components/alarm-templates/ImmersiveTemplate.tsx`
- Create: `src/components/alarm-templates/index.ts`
- Modify: `app/alarm-triggered.tsx` (use template from alarm data)

**Step 1: Create StandardTemplate**

Existing alarm-triggered screen logic, cleaned up as a component. Props:
```typescript
interface AlarmTemplateProps {
  verseText: string;
  verseReference: string;
  alarmName: string;
  onSilence: () => void;
  onClose: () => void;
  onSnooze: () => void;
}
```

**Step 2: Create IncomingCallTemplate**

Style: iOS incoming call. Circular icon at top (Ionicons `book` or category icon), alarm name as "caller ID", category subtitle, verse text below. Bottom: green circle button (Prier) + red circle button (Plus tard), small text "Rappeler dans 5 min". Subtle pulse animation on the icon using `Animated.loop`.

**Step 3: Create WakeUpTemplate**

Style: iPhone Clock alarm. Large current time at top (HH:MM, 64px thin weight). Verse below. "Snooze" button + "Arreter" button at bottom.

**Step 4: Create ImmersiveTemplate**

Style: Animated gradient background using expo-linear-gradient with `Animated` shifting colors. Verse centered with text shadow glow. Slide-to-pray gesture at bottom using `PanResponder` or `Animated` horizontal drag. After sliding: prayer mode with timer.

**Step 5: Create index barrel file**

```typescript
export { StandardTemplate } from './StandardTemplate';
export { IncomingCallTemplate } from './IncomingCallTemplate';
export { WakeUpTemplate } from './WakeUpTemplate';
export { ImmersiveTemplate } from './ImmersiveTemplate';
```

**Step 6: Update alarm-triggered to use templates**

Modify `app/alarm-triggered.tsx`:
- Read alarm data from params (including `template` field)
- Switch on template to render correct component
- Pass standardized props to each template

**Step 7: Verify and commit**

```bash
git add src/components/alarm-templates/ app/alarm-triggered.tsx
git commit -m "feat: implement 4 alarm display templates (standard, call, wake-up, immersive)"
```

---

## Task 6: Bible Data — Download and Structure getBible v2 JSON

**Files:**
- Create: `scripts/fetch-bible.ts` (Node script to download and format Bible data)
- Create: `assets/bible/ls1910/` (66 JSON files)
- Create: `assets/bible/kjv/` (66 JSON files)
- Create: `src/data/bible-meta.json` (book metadata)
- Create: `src/data/bible-translations.ts` (translation registry)

**Step 1: Write fetch script**

Create `scripts/fetch-bible.ts` — Node script that:
1. Fetches from `https://api.getbible.net/v2/ls1910.json` (full Louis Segond)
2. Fetches from `https://api.getbible.net/v2/kjv.json` (full KJV)
3. Splits each into per-book JSON files
4. Saves to `assets/bible/{translation}/{book}.json`
5. Format: `{ "1": { "1": "verse text", "2": "..." }, ... }` (chapter -> verse -> text)

Run: `npx ts-node scripts/fetch-bible.ts`

**Step 2: Create bible-meta.json**

66 books with id, name_fr, name_en, chapters count, testament (old/new).

**Step 3: Create translation registry**

```typescript
export interface BibleTranslation {
  key: string;
  name: string;
  language: 'fr' | 'en';
  bundled: boolean;
}

export const BIBLE_TRANSLATIONS: BibleTranslation[] = [
  { key: 'ls1910', name: 'Louis Segond 1910', language: 'fr', bundled: true },
  { key: 'kjv', name: 'King James Version', language: 'en', bundled: true },
  { key: 'darby_fr', name: 'Darby', language: 'fr', bundled: false },
  { key: 'martin', name: 'Martin 1744', language: 'fr', bundled: false },
  { key: 'asv', name: 'American Standard Version', language: 'en', bundled: false },
];
```

**Step 4: Commit**

```bash
git add assets/bible/ src/data/bible-meta.json src/data/bible-translations.ts scripts/
git commit -m "feat: add offline Bible data (Louis Segond 1910 + KJV) with metadata"
```

---

## Task 7: Thematic Verse Index

**Files:**
- Create: `src/data/verse-tags.json`
- Modify: `src/data/builtin-packs.ts` (replace with thematic category system)
- Modify: `src/storage/packs.ts` (load categories from verse-tags)

**Step 1: Create verse-tags.json**

JSON file with 14 categories, each containing 50-200 verse references mapped to book/chapter/verse:
```json
{
  "healing": [
    { "ref": "Esaie 53:5", "book": "isaiah", "chapter": 53, "verse": 5 },
    ...
  ],
  "encouragement": [ ... ],
  "prosperity": [ ... ],
  "faith": [ ... ],
  "protection": [ ... ],
  "love": [ ... ],
  "wisdom": [ ... ],
  "praise": [ ... ],
  "peace": [ ... ],
  "family": [ ... ],
  "work": [ ... ],
  "forgiveness": [ ... ],
  "strength": [ ... ],
  "joy": [ ... ]
}
```

This file is large (~2000+ entries). Build it by researching well-known thematic Bible verse lists.

**Step 2: Update builtin-packs.ts**

Replace hardcoded 100-verse packs with dynamic category system that resolves verses from verse-tags + active Bible translation at runtime.

**Step 3: Update packs storage**

Modify `getAllPacks()` to generate category packs dynamically from verse-tags.json, loading verse text from the active Bible translation.

**Step 4: Commit**

```bash
git add src/data/verse-tags.json src/data/builtin-packs.ts src/storage/packs.ts
git commit -m "feat: add thematic verse index with 14 categories and dynamic verse resolution"
```

---

## Task 8: Bible Browser UI

**Files:**
- Create: `app/(tabs)/packs/bible.tsx` (Bible browser screen)
- Create: `app/(tabs)/packs/bible-chapter.tsx` (chapter verse list)
- Modify: `app/(tabs)/packs/index.tsx` (add "Parcourir la Bible" button)
- Modify: `app/(tabs)/packs/_layout.tsx` (add bible routes)

**Step 1: Create Bible browser screen**

`app/(tabs)/packs/bible.tsx`:
- Header with translation dropdown
- Search bar
- Two sections: "Ancien Testament" / "Nouveau Testament"
- List of 66 books with chapter count
- Tap book -> push to bible-chapter with bookId + translationKey

**Step 2: Create chapter/verse screen**

`app/(tabs)/packs/bible-chapter.tsx`:
- Header: book name + chapter selector (horizontal pills)
- Verse list: each verse has text + "+" button
- "+" opens action sheet: choose which custom pack to add to
- Toast "Ajoute !" on success

**Step 3: Add Bible button to packs list**

In `app/(tabs)/packs/index.tsx`, add a button below the pack list:
```tsx
<Pressable onPress={() => router.push('/(tabs)/packs/bible')} style={[styles.bibleButton, { borderColor: colors.primary }]}>
  <Ionicons name="book-outline" size={20} color={colors.primary} />
  <Text style={[styles.bibleButtonText, { color: colors.primary }]}>{t('packs.browse_bible')}</Text>
</Pressable>
```

**Step 4: Verify and commit**

```bash
git add app/(tabs)/packs/bible.tsx app/(tabs)/packs/bible-chapter.tsx app/(tabs)/packs/index.tsx app/(tabs)/packs/_layout.tsx
git commit -m "feat: add Bible browser with book/chapter navigation and verse picking"
```

---

## Task 9: Audio Files + Updated Audio System

**Files:**
- Create: `assets/audio/*.mp3` (12 audio files)
- Modify: `src/data/builtin-audio.ts` (update with new sounds + i18n keys)
- Modify: `src/services/audio.ts` (update getBuiltinAudioFile switch)

**Step 1: Source audio files**

Download royalty-free/CC0 sounds:
- 7 notification sounds (3-5 seconds each): default, tri-tone, bell, chime, harp, shofar, gentle-ping
- 5 worship/ambient tracks (30-60 seconds): worship-piano-1, worship-piano-2, gentle-worship, prayer-ambient, meditation-calm

Sources: freesound.org, pixabay.com/music (CC0), or generate with AI tools.

**Step 2: Update builtin-audio.ts**

Add shofar and gentle-ping to NATIVE_SOUNDS. Use i18n keys for names.

**Step 3: Update audio.ts switch**

Add cases for new sounds in `getBuiltinAudioFile()`.

**Step 4: Commit**

```bash
git add assets/audio/ src/data/builtin-audio.ts src/services/audio.ts
git commit -m "feat: add native alarm sounds and worship audio tracks"
```

---

## Task 10: Final Integration + Fixes

**Files:**
- All modified files
- Modify: `app.json` (version bump to 1.1.0)

**Step 1: Fix pending bugs from previous tasks**

Run the app, test each flow:
- Create alarm with custom interval
- Change language FR <-> EN
- Browse Bible, add verse to pack
- Trigger alarm with each template
- Verify alarm fires reliably at intervals

**Step 2: Version bump**

Update `app.json` and `package.json` version to `1.1.0`.

**Step 3: Final commit and push**

```bash
git add -A
git commit -m "feat: Prayer Room v1.1 — i18n, Bible, templates, custom intervals"
git push origin main
```
